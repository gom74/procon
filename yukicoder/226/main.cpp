#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
    cin.tie(0);
    ios::sync_with_stdio(false);

    int H, W;
    cin >> H >> W;
    vector<string> V(H);
    for (int i = 0; i < H; ++i) {
        cin >> V.at(i);
    }

    return 0;
}